<div class="card numan">
    <img class="icon @(PadImage ? "pad" : "")" src="@ImageSource" />
    <p>@Text</p>
</div>

@code {
    [Parameter]
    public string ImageSource { get; set; }
    [Parameter]
    public string Text { get; set; }

    [Parameter]
    public bool PadImage { get; set; }
}